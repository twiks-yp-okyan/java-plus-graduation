kafka:
  bootstrap-servers: "localhost:9092"
  auto-offset-reset: earliest
  consumer:
    poll-timeout: 100ms
    group-id: aggregator-group
    topic: stats.user-actions.v1
  producer:
    topic: stats.events-similarity.v1

logging:
  level:
    ru.yandex.practicum.ewm.aggregator: DEBUG