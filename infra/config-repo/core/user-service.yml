spring:
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:6543/user-db
    username: login
    password: pass

server:
  port: 0

logging:
  level:
    ru.yandex.practicum.user: DEBUG